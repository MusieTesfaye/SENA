[package]
name = "sena"
version = "0.1.0"
authors = ["SENA Network contributors"]
license = "Apache-2.0"

[addresses]
sena = "_"

# Pinned to a commit rather than a branch.
#
# `rev = "mainnet"` is a moving target: the framework it resolves to changes
# under the package, and a build that cannot be reproduced cannot be the basis
# for a deployed package hash (BETA_BUILD_PLAN.md W-02).
#
# This commit is the tree tagged `aptos-cli-v7.9.0`, chosen so the framework and
# the CLI that compiles it come from the same revision. Later framework
# revisions use Move 2 syntax that CLI 7.9.0 cannot parse, and later CLIs do not
# run on every machine -- see move/README.md.
[dependencies.AptosFramework]
git = "https://github.com/aptos-labs/aptos-core.git"
rev = "46d871fa1feb61ffafb73353a0755e8cc3aaed9d"
subdir = "aptos-move/framework/aptos-framework"
